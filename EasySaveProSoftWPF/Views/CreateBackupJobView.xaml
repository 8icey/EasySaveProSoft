<Window x:Class="EasySaveProSoft.WPF.Views.CreateBackupJobView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Backup Job" Height="350" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Name Label and TextBox -->
        <TextBlock Text="Name:" FontWeight="Bold" Margin="5"/>
        <TextBox x:Name="NameTextBox" Margin="5" Grid.Row="1" />

        <!-- Source Path Label and TextBox -->
        <TextBlock Text="Source Path:" FontWeight="Bold" Margin="5" Grid.Row="2"/>
        <TextBox x:Name="SourceTextBox" Margin="5" Grid.Row="3" />

        <!-- Destination Path Label and TextBox -->
        <TextBlock Text="Destination Path:" FontWeight="Bold" Margin="5" Grid.Row="4"/>
        <TextBox x:Name="TargetTextBox" Margin="5" Grid.Row="5" />

        <!-- Backup Type Label and ComboBox -->
        <TextBlock Text="Backup Type:" FontWeight="Bold" Margin="5" Grid.Row="6"/>
        <ComboBox x:Name="TypeComboBox" Grid.Row="7" Margin="5">
            <ComboBoxItem Content="Full" />
            <ComboBoxItem Content="Differential" />
        </ComboBox>

        <!-- Buttons -->
        <StackPanel Orientation="Horizontal" Grid.Row="8" HorizontalAlignment="Center" Margin="5">
            <Button Content="Create" Width="100" Margin="5" Click="Create_Click" />
            <Button Content="Cancel" Width="100" Margin="5" Click="Cancel_Click" />
        </StackPanel>
    </Grid>
</Window>
