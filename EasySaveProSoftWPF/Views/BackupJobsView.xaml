<UserControl x:Class="EasySaveProSoft.WPF.Views.BackupJobsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!-- For create inputs -->
            <RowDefinition Height="*" />
            <!-- ListBox -->
            <RowDefinition Height="Auto" />
            <!-- Progress Bar -->
            <RowDefinition Height="Auto" />
            <!-- Buttons -->
        </Grid.RowDefinitions>

        <!-- Create Job Inputs -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10" VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                <TextBlock Text="Name:" Width="120" Margin="5" VerticalAlignment="Center"/>
                <TextBlock Text="Source Path:" Width="180" Margin="5" VerticalAlignment="Center"/>
                <TextBlock Text="Target Path:" Width="180" Margin="5" VerticalAlignment="Center"/>
                <TextBlock Text="Type:" Width="120" Margin="5" VerticalAlignment="Center"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBox Width="120" Margin="5"  Text="{Binding NewJobName, UpdateSourceTrigger=PropertyChanged}" />
                <TextBox Width="180" Margin="5"  Text="{Binding NewJobSource, UpdateSourceTrigger=PropertyChanged}" />
                <TextBox Width="180" Margin="5"  Text="{Binding NewJobTarget, UpdateSourceTrigger=PropertyChanged}" />
                <ComboBox Width="120" Margin="5"
                          ItemsSource="{Binding BackupTypes}"
                          SelectedItem="{Binding NewJobType, UpdateSourceTrigger=PropertyChanged}" />
                <Button Content="Create Job" Width="100" Margin="5" Command="{Binding CreateBackupJobCommand}" />
            </StackPanel>
        </StackPanel>

        <!-- List of Backup Jobs -->
        <ListBox Grid.Row="1" ItemsSource="{Binding BackupJobs}" 
                 SelectedItem="{Binding SelectedBackupJob}" 
                 DisplayMemberPath="Name" 
                 Margin="5" />

        <!-- Progress Bar -->
        <ProgressBar x:Name="ExecutionProgressBar" Height="25" Grid.Row="2" Margin="5" Value="{Binding ProgressValue}" Minimum="0" Maximum="100" />

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">
            <Button Content="Execute Job" Width="100" Margin="5" Command="{Binding ExecuteBackupJobCommand}" />
            <Button Content="Execute All" Width="100" Margin="5" Command="{Binding RunAllBackupsCommand}" />
            <Button Content="Delete Job" Width="100" Margin="5" Command="{Binding DeleteBackupJobCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
